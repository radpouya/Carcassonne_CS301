<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_help"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/backButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/holo_purple"
        android:text="Go Back to Game"
        android:textColor="@android:color/background_light"
        android:textSize="24sp" />

    <ScrollView android:id="@+id/textAreaScroller"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical">
        <!--
             External Citation
             * Date: 25 April 2017
             * Problem: Too much text for a TextView to handle, so needed it
                        to be scrollable.
             * Resource: http://stackoverflow.com/questions/14434433/
                         androidtextview-with-vertical-scrolling-text
             * Solution: Used the first commenter's solution and added a
                         scrollbar in the XML
        -->

    <TextView
        android:id="@+id/helpTV"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:scrollbars="vertical"
        android:text="TextView"
        android:textColor="@android:color/background_light"
        android:textSize="24sp"
        />
    </ScrollView>

</LinearLayout>